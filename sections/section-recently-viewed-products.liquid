{%- liquid
  if section.settings.text_alignment == 'center'
    assign title_classes = 'text-center align-center'
  endif
-%}

{%- if section.settings.title != empty -%}
  <header class="cols">
    <div
      class="
        {{ title_classes }} title-styling
        {% if section.settings.title_underline_style != 'none' %}
          title-underline-none
          {% if section.settings.title_underline_style contains 'accent' %}
            title-underline-accent
          {% elsif section.settings.title_underline_style contains 'gradient' %}
            title-underline-gradient
          {% endif %}
          {% if section.settings.title_underline_style contains 'secondary_font' %}
            title-underline-secondary-font
          {% endif %}
        {% endif %}
      "
    >
      {%- if section.settings.title != empty -%}
        {{ section.settings.title }}
      {%- endif -%}
    </div>
  </header>
{%- endif -%}
<ul
  class="l4cl section-width-{{ section.settings.width }}"
  data-number_of_items="{{ section.settings.number_of_items }}"
  data-template="product-item"
  {% if section.settings.enable_quick_buy_desktop %}
    data-enable_quick_buy_desktop
  {% endif %}
  {% if section.settings.enable_quick_buy_mobile %}
    data-enable_quick_buy_mobile
  {% endif %}
  {% if section.settings.enable_quick_buy_qty_selector %}
    data-enable_quick_buy_qty_selector
  {% endif %}
  {% if section.settings.enable_quick_buy_compact %}
    data-enable_quick_buy_compact
  {% endif %}
  {% if section.settings.enable_quick_buy_drawer %}
    data-enable_quick_buy_drawer
  {% endif %}
  {% if section.settings.enable_color_picker %}
    data-enable_color_picker
  {% endif %}
></ul>

<div class="vanaya-attention-fallback" data-vanaya-attention-fallback hidden>
  <ul class="vanaya-attention-fallback__grid">
    {% assign fallback_count = 0 %}
    {% for fallback_product in collections.all.products %}
      {% if fallback_product.available and fallback_count < section.settings.number_of_items %}
        <li class="vanaya-attention-fallback__item">
          <a href="{{ fallback_product.url }}">
            {% if fallback_product.featured_image %}
              {{ fallback_product.featured_image | image_url: width: 700 | image_tag: loading: 'lazy', alt: fallback_product.featured_image.alt | default: fallback_product.title }}
            {% endif %}
            <span>{{ fallback_product.title }}</span>
          </a>
        </li>
        {% assign fallback_count = fallback_count | plus: 1 %}
      {% endif %}
    {% endfor %}
  </ul>
</div>

<script>
document.addEventListener('DOMContentLoaded', function(){
  var section=document.getElementById('shopify-section-{{ section.id }}');
  if(!section) return;
  var dynamicList=section.querySelector('.l4cl');
  var fallback=section.querySelector('[data-vanaya-attention-fallback]');
  if(!dynamicList || !fallback) return;
  function syncFallback(){
    fallback.hidden = dynamicList.children.length > 0;
  }
  async function useShopFeaturedImages(){
    var cards=Array.from(dynamicList.querySelectorAll('.product-card'));
    for(var i=0;i<cards.length;i++){
      var card=cards[i];
      if(card.dataset.shopImageSynced==='1') continue;
      var link=card.querySelector('h3 a[href], figure a[href]');
      var img=card.querySelector('figure img');
      if(!link||!img) continue;
      try{
        var url=new URL(link.href,window.location.origin);
        var res=await fetch(url.pathname+'.js',{credentials:'same-origin'});
        if(!res.ok) continue;
        var product=await res.json();
        if(product.featured_image){
          var featured=product.featured_image;
          if(featured.indexOf('//')===0) featured=window.location.protocol+featured;
          img.removeAttribute('srcset');
          img.src=featured;
          img.style.setProperty('object-fit','contain','important');
          img.style.setProperty('object-position','center','important');
          card.dataset.shopImageSynced='1';
        }
      }catch(e){}
    }
  }
  function refreshCards(){
    syncFallback();
    useShopFeaturedImages();
  }
  setTimeout(refreshCards,1200);
  setTimeout(refreshCards,2400);
  new MutationObserver(refreshCards).observe(dynamicList,{childList:true,subtree:true});
});
</script>

<style>
  #shopify-section-{{ section.id }} { position: relative; z-index: {{ section.settings.fix_zindex }}!important; }
  #shopify-section-{{ section.id }} .l4cl{
    display:grid!important;
    grid-template-columns:repeat(4,minmax(0,1fr))!important;
    gap:24px!important;
    margin-bottom:{{ section.settings.spacing_desktop | minus: 22 }}px;
    padding-left:0!important;
  }
  #shopify-section-{{ section.id }} .l4cl>li,
  #shopify-section-{{ section.id }} .l4cl .product-card{
    width:100%!important;
    max-width:none!important;
    margin:0!important;
  }
  #shopify-section-{{ section.id }} .l4cl .product-card>figure{
    aspect-ratio:1/1!important;
    overflow:hidden!important;
  }
  #shopify-section-{{ section.id }} .l4cl .product-card>figure>a,
  #shopify-section-{{ section.id }} .l4cl .product-card>figure picture{
    display:block!important;
    width:100%!important;
    height:100%!important;
  }
  #shopify-section-{{ section.id }} .l4cl .product-card>figure img{
    width:100%!important;
    height:100%!important;
    object-fit:contain!important;
    object-position:center!important;
  }
  @media only screen and (max-width: 47.5em) {
    #shopify-section-{{ section.id }} .l4cl{
      grid-template-columns:repeat(2,minmax(0,1fr))!important;
      gap:12px!important;
      margin-bottom:{{ section.settings.spacing_mobile | minus: 8 }}px;
    }
  }

  #shopify-section-{{ section.id }} .vanaya-attention-fallback__grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:18px;margin:0 0 {{ section.settings.spacing_desktop | minus: 22 }}px;padding:0;list-style:none}
  #shopify-section-{{ section.id }} .vanaya-attention-fallback__item a{display:block;color:inherit;text-decoration:none}
  #shopify-section-{{ section.id }} .vanaya-attention-fallback__item img{display:block;width:100%;aspect-ratio:4/5;object-fit:contain;object-position:center;background:#faf7ef}
  #shopify-section-{{ section.id }} .vanaya-attention-fallback__item span{display:block;padding-top:10px;font-size:13px;line-height:1.4}
  @media only screen and (max-width:47.5em){#shopify-section-{{ section.id }} .vanaya-attention-fallback__grid{grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-bottom:{{ section.settings.spacing_mobile | minus: 8 }}px}}
</style>

{% schema %}
{
  "name": "t:sections.recently_viewed_products.name",
  "tag": "article",
  "class": "recently-viewed-products",
  "disabled_on": {
    "templates": ["gift_card", "password"],
    "groups": ["header", "custom.overlay"]
  },
  "settings": [
    {
      "type": "range",
      "id": "number_of_items",
      "label": "t:sections.recently_viewed_products.settings.number_of_items.label",
      "info": "t:sections.recently_viewed_products.settings.number_of_items.info",
      "min": 2,
      "max": 6,
      "step": 1,
      "default": 5
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "t:sections.recently_viewed_products.settings.text_alignment.label",
      "options": [
        {
          "value": "start",
          "label": "t:sections.recently_viewed_products.settings.text_alignment.options__1.label"
        },
        {
          "value": "center",
          "label": "t:sections.recently_viewed_products.settings.text_alignment.options__2.label"
        }
      ],
      "default": "start"
    },
    {
      "type": "select",
      "id": "width",
      "label": "t:global.layout.width.label",
      "options": [
        {
          "value": "boxed",
          "label": "t:global.layout.width.options__1.label"
        },
        {
          "value": "wide",
          "label": "t:global.layout.width.options__2.label"
        }
      ],
      "default": "boxed",
      "visible_if": "{{ settings.width < 2000 }}"
    },
    {
      "type": "header",
      "content": "t:global.typography.title.label"
    },
    {
      "type": "richtext",
      "id": "title",
      "label": "t:global.typography.title.label",
      "info": "t:global.typography.title.info",
      "default": "<h2>Recently viewed products</h2>"
    },
    {
      "type": "select",
      "id": "title_underline_style",
      "label": "t:global.typography.title_underline_style.label",
      "options": [
        {
          "value": "none",
          "label": "t:global.typography.title_underline_style.none.label"
        },
        {
          "value": "secondary_font",
          "label": "t:global.typography.title_underline_style.secondary_font.label",
          "group": "t:global.typography.title_underline_style.group.fonts"
        },
        {
          "value": "secondary_font_accent",
          "label": "t:global.typography.title_underline_style.secondary_font_accent.label",
          "group": "t:global.typography.title_underline_style.group.fonts"
        },
        {
          "value": "secondary_font_gradient",
          "label": "t:global.typography.title_underline_style.secondary_font_gradient.label",
          "group": "t:global.typography.title_underline_style.group.fonts"
        },
        {
          "value": "accent",
          "label": "t:global.typography.title_underline_style.accent.label",
          "group": "t:global.typography.title_underline_style.group.colors"
        },
        {
          "value": "gradient",
          "label": "t:global.typography.title_underline_style.gradient.label",
          "group": "t:global.typography.title_underline_style.group.colors"
        }
      ]
    },
    {
      "type": "header",
      "content": "t:global.quick_buy.header"
    },
    {
      "type": "paragraph",
      "content": "t:global.quick_buy.paragraph"
    },
    {
      "id": "enable_quick_buy_desktop",
      "type": "checkbox",
      "label": "t:global.quick_buy.enable_quick_buy_desktop.label",
      "default": true
    },
    {
      "id": "enable_quick_buy_mobile",
      "type": "checkbox",
      "label": "t:global.quick_buy.enable_quick_buy_mobile.label",
      "default": true
    },
    {
      "id": "enable_quick_buy_drawer",
      "type": "checkbox",
      "label": "t:global.quick_buy.enable_quick_buy_drawer.label",
      "info": "t:global.quick_buy.enable_quick_buy_drawer.info",
      "visible_if": "{{ section.settings.enable_quick_buy_desktop or section.settings.enable_quick_buy_mobile }}"
    },
    {
      "id": "enable_quick_buy_qty_selector",
      "type": "checkbox",
      "label": "t:global.quick_buy.enable_quick_buy_qty_selector.label",
      "info": "t:global.quick_buy.enable_quick_buy_qty_selector.info",
      "default": true,
      "visible_if": "{{ section.settings.enable_quick_buy_desktop or section.settings.enable_quick_buy_mobile }}"
    },
    {
      "id": "enable_color_picker",
      "type": "checkbox",
      "label": "t:global.quick_buy.enable_color_picker.label",
      "visible_if": "{{ section.settings.enable_quick_buy_desktop or section.settings.enable_quick_buy_mobile }}"
    },
    {
      "id": "enable_quick_buy_compact",
      "type": "checkbox",
      "label": "t:global.quick_buy.enable_quick_buy_compact.label",
      "info": "t:global.quick_buy.enable_quick_buy_compact.info",
      "visible_if": "{{ section.settings.enable_quick_buy_desktop or section.settings.enable_quick_buy_mobile }}"
    },
    {
      "type": "header",
      "content": "t:global.spacing.header"
    },
    {
      "id": "spacing_desktop",
      "type": "range",
      "label": "t:global.spacing.spacing_desktop.label",
      "min": -250,
      "max": 250,
      "step": 5,
      "unit": "px",
      "default": 50
    },
    {
      "id": "spacing_mobile",
      "type": "range",
      "label": "t:global.spacing.spacing_mobile.label",
      "min": -250,
      "max": 250,
      "step": 5,
      "unit": "px",
      "default": 30
    },
    {
      "id": "fix_zindex",
      "type": "range",
      "label": "t:global.fix_zindex.label",
      "info": "t:global.fix_zindex.info",
      "min": 0,
      "max": 25,
      "step": 1,
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "t:sections.recently_viewed_products.presets.name",
     "category": "t:categories.products"
    }
  ]
}
{% endschema %}
